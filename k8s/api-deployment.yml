apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-deployment
  labels:
    app: api
spec:
  replicas: 3
  selector:
    matchLabels:
      app: api
  template:
    metadata:
      labels:
        app: api
    spec:
      containers:
      - name: api
        image: learnx/api:latest
        ports:
        - containerPort: 8000
        env:
        - name: ENVIRONMENT
          value: "production"
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: learnx-secrets
              key: database_url
        - name: REDIS_URL
          valueFrom:
            secretKeyRef:
              name: learnx-secrets
              key: redis_url
        - name: S3_ENDPOINT
          valueFrom:
            configMapKeyRef:
              name: learnx-config
              key: s3_endpoint
        - name: S3_ACCESS_KEY
          valueFrom:
            secretKeyRef:
              name: learnx-secrets
              key: s3_access_key
        - name: S3_SECRET_KEY
          valueFrom:
            secretKeyRef:
              name: learnx-secrets
              key: s3_secret_key
        - name: S3_BUCKET
          valueFrom:
            configMapKeyRef:
              name: learnx-config
              key: s3_bucket
        - name: OPENAI_API_KEY
          valueFrom:
            secretKeyRef:
              name: learnx-secrets
              key: openai_api_key
        resources:
          limits:
            cpu: "1000m"
            memory: "1Gi"
          requests:
            cpu: "500m"
            memory: "512Mi"
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 15
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: api-service
spec:
  selector:
    app: api
  ports:
  - port: 80
    targetPort: 8000
  type: ClusterIP
